<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dreamerlxb.recyclerviewdemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".rv.fragments.ScaleHeaderActivity" />
        <activity android:name=".rxjava.MainActivity" />
        <activity android:name=".lottie.MainActivity" />
        <activity android:name=".rv.MainActivity" />
        <activity android:name=".notifications.MainActivity" />
        <activity android:name=".editor.MainActivity" />
        <activity android:name=".rxjava.LifecycleTestActivity"></activity>
    </application>

</manifest>